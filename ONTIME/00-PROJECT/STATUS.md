# OnTime — Project Status

Hub: [[OnTime]] · Work order: [[ROADMAP]].

Last verified against source: 2026-06-29

## Backend — implemented & wired (controller + service, all C#/EF Core)
- [x] Clean architecture (Domain / Application / Infrastructure / API)
- [x] All entities + EF configurations
- [x] All query/aggregate/write logic in C#/EF Core LINQ — no PostgreSQL stored functions, `DatabaseFunctions.cs` retired (see [[2026-06-29-data-layer-migrated-to-csharp]])
- [x] `DatabaseInitializer` (EnsureCreated + drift detection + auto-recreate)
- [x] Auth: register manager, register salesperson, login, public company/brand lookup
- [x] Clients: list (manager sees brand), create, hot deals, detail, delete, stage change, history, sales history
- [x] Proposals: list, detail, update, create-for-client, mark lost, convert to sale
- [x] Sales: list, detail, dashboard
- [x] Notifications: list, today, overdue-count, create, done/snooze/ignore (PATCH)
- [x] Notification preferences: get, update (`/api/preferences/notifications`)
- [x] Stages: CRUD, reorder, templates CRUD
- [x] Vehicles: brands, models, versions (manager-only writes)
- [x] Users: me, update me, public profile; manager: list/get/invite/activate/view-dashboard
- [x] **Goals API** — full CRUD, live progress from KPIs (`UserGoalService`)
- [x] **Permissions API** — seed + update, manager-only (`PermissionService`)
- [x] **Friends API** — requests, accept/reject, remove, public profile (`FriendshipService`)
- [x] Subscription: status + cancel work (stub); see below
- [x] Admin: companies + brands CRUD (manager-only)
- [x] Subscription middleware (402/403 enforcement) + error-handling middleware
- [x] Integration tests (xUnit + Testcontainers + Respawn + WireMock). See [[TESTING]]. **154/154 green (2026-06-25)**
- [x] **Vehicle status + active toggle** (T15): `IsActive`/`IsConfigured` on `VehicleModelListDto`; `PATCH /api/vehicles/models/{id}/active`. See [[VEHICLES]].
- [x] **User Brands filter** (F1): `UserVehicleBrand` join + `GET/PUT /api/users/me/vehicle-brands` + server-side filtering of `GET /api/vehicles/models`. See [[USER-BRANDS]].
- [x] **Proposal vehicle requirement** (T14): proposals (inline at client creation, or standalone) now require ≥1 vehicle — `PROPOSAL_MISSING_VEHICLE` (422) at creation instead of failing later at convert-to-sale.
- [x] **Client stage fallback** (2026-06-24): client creation now falls back to the lowest-order stage even if all stages were deactivated, so `/api/clients` no longer throws `STAGE_NOT_FOUND` in that case.
- [x] **Proposal and sale edit flows** (2026-06-24): proposal detail now opens an edit modal for value, dates, vehicles and trade-in data; sales now open a detail page instead of bouncing to the dashboard and allow editing commission/value/date/vehicle fields.
- [x] **Shared frontend API types split** (2026-06-24): `src/types/api.ts` is now a barrel file and the DTOs are split by domain into `src/types/api-*.ts` so the type file stays under the line limit.
- [x] **New-client vehicle picker** (2026-06-24): brand selector locks when the user has exactly one selected vehicle brand; if there are multiple, the user can choose between them; only active models are shown in that flow.
- [x] **Frontend regressions revalidated** (2026-06-24): theme flash, raw i18n keys, and proposal back-navigation were re-fixed after source drift; current build is green.
- [x] **Sales list vehicle name** (2026-06-24): `SalesPage` showed `—` for sales with a free-text vehicle (no catalog `Model`) because it only read `modelName`. Added `saleVehicleName()` helper (`src/utils/sales.ts`) falling back to `freeTextModel`; wired into desktop/tablet columns + mobile card. 3 new Vitest tests.
- [x] **Test infra fix** (2026-06-24): `TestWebAppFactory.ResetDatabaseAsync` excluded `vehicle_brands` from Respawn but not `vehicle_models`, so models were wiped every test after the first — any test needing a seeded `VehicleModel` failed with "Sequence contains no elements". Now reseeds models whenever empty, independent of brand existence. Also fixed `ProposalSaleFlowTests.ConvertProposalToSale_UsesProposalVehicleWhenRequestOmitsVehicle`, which asserted `ModelName` without the brand prefix even though `SaleRepository` has always returned `"{Brand} {Model}"`. 129/129 backend tests green.
- [x] **Proposal/client/sale field-name drift fix** (2026-06-24, found via live browser testing): frontend types had drifted from the backend contract in several places, causing values to silently render blank or `0,00 €`:
  - `ProposalListDto`/`ProposalDto.value` → backend sends `proposalValue`. Fixed in `types/api-proposal.ts`, `ProposalDetailPage.tsx`, `ProposalsPage.tsx`, `ClientDetailPage.tsx`.
  - `ProposalVehicleDto.brandName` → backend sends `modelBrandName`; also added the missing `id`/`freeTextModel` fields and made `modelId`/`modelName` optional (free-text vehicles have neither).
  - **Convert-to-Sale modal had no `finalValue` input at all** — it silently sent `undefined`, which the backend's non-nullable `decimal FinalValue` deserialized as `0`. This is why every converted sale showed `0,00 €`. Added the missing field, defaulting to the proposal's value.
  - `ClientDto.activeProposal` — backend never returns this field; removed the dead "Proposta Activa" card from `ClientDetailPage.tsx` Info tab (the data the user expected to see here is in the Propostas tab, which works).
  - `StageHistoryDto` (frontend) had `stageName`/`notes`, but backend sends `fromStageName`/`toStageName`/`toStageColor`/`obs` — the Histórico tab Timeline was rendering nothing but the date. Fixed to show `"{FromStage} → {ToStage}"` + `obs`.
  - `ClientSaleHistoryDto` (frontend) had `value`/`vehicleName`/`createdAt`, but backend sends `finalValue`/`modelName`+`freeTextModel`/no `createdAt`. Fixed the Vendas tab on `ClientDetailPage.tsx` accordingly.
  - Regression test added: `src/__tests__/utils/proposalValue.test.ts`.
- [x] **Notification settings mojibake** (2026-06-24): `NotificationSettingsPage.tsx` had literal corrupted bytes saved in the source (`'â€”'` instead of `'—'`, and `â”€` box-drawing mojibake in section-divider comments) — this was a real encoding bug, distinct from the `/api/i18n` payload (which was already correct). Fixed by rewriting the file as proper UTF-8.
- [x] **Friend search with autocomplete** (2026-06-24, feature request): "Adicionar Amigo" previously only took an exact email. Added `GET /api/friends/search?q=` (name or email, partial match, excludes self, flags `alreadyFriend`/`requestPending`); `SendFriendRequestDto` now accepts either `email` or `userId`. Frontend: `FriendsPage.tsx` add-friend modal replaced with a debounced AntD `AutoComplete`, each result shows a hover `Popover` with email/brand/company, and a confirmation chip before sending. New `FriendsFlowTests.cs` (3 tests, previously no Friends flow coverage existed at all).
- [x] **Stage edit form was a no-op for Active/Final/Won/Lost** (2026-06-24, found via live browser testing): `UpdateStageRequest` (backend) never had `IsActive`/`IsFinal`/`IsWon`/`IsLost` fields at all — the frontend `StagesPage` form collected these toggles but they were silently dropped by JSON binding, and **`IsActive` defaulted to `false`** on every edit (since the frontend didn't send it either), meaning editing *any* stage's name/color silently deactivated it. Fixed: added the fields to `UpdateStageRequest` + `ClientStageService.UpdateAsync`; only one stage can be `IsWon` and one `IsLost` per user — setting either now auto-demotes the previous holder (`STAGE_WON_AND_LOST` 422 if both requested on the same stage). Also: `ClientStageDto.ClientCount` didn't exist on the backend at all (frontend read `stage.clientCount` from nothing, always `undefined`) — the "N clientes" tag rendered blank and, more importantly, `undefined > 0` always evaluated false so the delete button's "has active clients" guard never actually blocked anything client-side (backend still rejected via `STAGE_HAS_CLIENTS`, so no data was at risk, but the UX never warned upfront). Added real `ClientCount` per stage in `StageRepository.GetByUserAsync`. Frontend: `StagesPage.tsx` color field now uses AntD `ColorPicker` (was a raw hex `Input`), added the missing "Ativa" toggle, mutual-exclusion UI for Won/Lost, and a link to Notification Settings for managing each stage's automatic notification templates (which are not editable from this page). 4 new backend tests (`StageConfigFlowTests`).
- [x] **Dashboard "Negócios Quentes" field-name drift** (2026-06-24): `DashboardDto.hotDeals` was typed as `ClientListDto[]` but the backend returns a distinct lean shape (`fullName`, `stageName`, `stageColor`, `lastInteractionAt` — no `temperature`). The "Etapa" column read `currentStageName` (always blank) and the "Temp." column/filter read `temperature` (which doesn't exist on this DTO), so `tempKey(undefined)` resolved to the literal fallback key `'MSG.EMPTY_STATE.DEFAULT'`, rendering "Sem resultados." inside the Temp. cell. Fixed: added `HotDealDto` type, fixed the Etapa `dataIndex`, removed the broken/meaningless temperature filter (hot deals are always `Temperature=Hot` by definition — there's nothing to filter), Temp. column now renders a static Hot tag.
- [x] **VehicleProposalTable: Marca blank when editing an existing proposal** (2026-06-24): when opening "Editar" on a proposal whose vehicle already has a `modelId` but the row's `brandId` was never set (only inferred from `brandIds` when the user has exactly one selected brand), the Marca dropdown showed empty even though Modelo correctly showed the model name. Added a `BrandSelect` wrapper that infers the brand from the model (mirroring `ModelSelect`'s existing pattern) without clearing the model selection.
- [x] **Mobile responsiveness pass** (2026-06-24, verified live at real mobile viewport widths — see [[CONVENTIONS]] "Mobile / responsive patterns" for the extracted rules): fixed hardcoded `bg-white` mobile cards breaking dark mode (Sales/Proposals/Clients/Brands → switched to AntD `Card`); page headers (title + button) wrapping the title into 3 broken lines with the button overlapping it (every `ResourcePage`-based list, plus Stages/Friends/Goals/Admin — fixed with `flex-wrap` + `whitespace-nowrap` on the title); `<Descriptions>` wrapping dates one character per line on narrow screens (Proposal/Sale/Client/Subscription detail — fixed with `layout={isMobile ? 'vertical' : 'horizontal'}`); `BottomNav` clipping 2 of its 5 tabs off-screen (labels didn't shrink — added `min-w-0` + `truncate`); triple-stacked horizontal padding making content feel cramped (now lives once, in `AppLayout`); Dashboard's hot-deals table forcing the whole page to shrink-to-fit on mobile because it had no `scroll={{x}}`. Also added a `commission` field to the Convert-to-Sale modal (was only editable after the fact, via Sale → Editar).
- [x] **Won/Lost clients still showed Temperature ("Quente")** (2026-06-25): `ClientListDto` never exposed the current stage's `IsFinal`/`IsWon`/`IsLost` flags, so the frontend had no way to tell a closed deal from an open one — a sold client kept showing the red "Quente" urgency tag forever. Added the 3 flags end-to-end (`fn_get_clients_paged` SQL function → `ClientPagedRow` → `ClientListDto`); new `TemperatureOrOutcomeTag` component renders "Venda"/"Perdido" instead of the temperature once the stage is final. Applied to `ClientsPage` (desktop columns + mobile card) and `ClientDetailPage`. 1 new backend test.
- [x] **Dashboard "Negócios Quentes" search input overlapping the table on mobile** (2026-06-25): the `styles={{ header: { flexWrap: 'wrap' } }}` fix from the previous mobile pass targeted the wrong AntD `Card` internal element — `title`/`extra` are laid out by `.ant-card-head-wrapper`, not `.ant-card-head`. Rebuilt the header as a single custom flex-wrap `<div>` passed entirely as `title` (no `extra`) so it actually wraps on narrow screens.
- [x] **Security hardening** (2026-06-25, see [[SECURITY]] for detail): PBKDF2 raised from 10k → 600k iterations (iteration count now embedded per-hash, backward compatible with existing passwords); login rate limited to 5/min per IP (429 beyond that); `/api/friends/search` now requires ≥2 characters and masks email in results (`j***@example.com`) since it's an intentionally cross-tenant, unauthenticated-to-each-other directory search.
- [x] **Goals progress was always "this calendar month" regardless of period** (2026-06-25): `UserGoalService` reused `dashboard.SalesThisMonth`/`ProposalsThisMonth`/`ActiveClients`/`ConversionRate` for every goal, so Daily/Weekly/Annual goals (and NewClients, which mapped to lifetime active-client count) showed numbers with no relation to their actual window. Rewrote to compute current value by counting Sales/Proposals/Clients directly from the DB scoped to `[StartDate, EndDate ?? PeriodEnd)`. Added `GoalPeriod.Annual`. Added `UserGoal.ShowOnDashboard` (+ `CreateUserGoalRequest`/`UpdateUserGoalRequest`/DTO) so a goal can be pinned to a new "Os Meus Objetivos" widget on `DashboardPage` — goals never appeared anywhere outside `/goals` before. 2 new backend tests (period-scoped progress, Annual + pin flag).
- [x] **`ProtectedRoute managerOnly` blocked Admin from Marcas/Admin/Controlo de Acesso** (2026-06-25): checked `role !== 1` only; Admin (role=2) saw the menu items (after a prior session's Sidebar fix) but was bounced back to `/dashboard` on click. Now matches the Sidebar's own `role 1 || 2` check.
- [x] **Friend request card unreadable in dark mode** (2026-06-25): `FriendsPage`'s pending-request `<List>` hardcoded `background: '#fff'`, so the sender's name/email rendered as light-on-white-on-dark-background — i.e. invisible. Switched to the theme token.
- [x] **Generic schema-drift detection** (2026-06-25): `DatabaseInitializer.IsSchemaCurrentAsync` checked one hardcoded sentinel column; replaced with a check of every table+column the current EF model expects. See [[KNOWN-ISSUES]] for the one-time local-data-wipe side effect of finally catching real accumulated drift.
- [x] **Sidebar / mobile drawer redesign** (2026-06-24): `Sidebar.tsx` rewritten from a flat 12-item icon list into 4 labelled groups (Geral / Vendas / Operação / Configuração) using AntD `Menu` item groups, with a branded header (colour-mark "OT" + "OnTime" + the user's brand name, using `user.brandColor` as the accent) and a proper user footer (avatar, name, role). Takes a `variant: 'sider' | 'drawer'` prop: the desktop rail (`AppLayout`'s `Layout.Sider`, 232px) uses a click-to-open dropdown for profile/logout; the mobile `Drawer` variant shows an explicit close (✕) button and a visible logout icon-button instead of a dropdown (better touch ergonomics), and now **closes itself when a nav item is clicked** (`onNavigate` callback) — previously the drawer stayed open over the new page until manually dismissed. Also fixed `isManager` only checking `role === 1`, which hid manager-only nav items (Marcas/Admin/Controlo de Acesso) from Admin (`role === 2`) accounts — now `role === 1 || role === 2`. New i18n keys: `NAV.GROUP.OVERVIEW/SALES/WORKSPACE/SETTINGS`.
- [x] **Desktop sidebar collapse polish** (2026-06-25, several false starts): collapsing now keeps the nav icon at a fixed column position the whole time (`padding-left: 16px` forced via CSS regardless of antd's own collapsed-mode layout, which otherwise jumps it once after the fact) and only the label text fades — never re-toggles antd's `inlineCollapsed` at all, so there's no separate "finishing" step. Hover tooltips on collapsed items render real text again (the fade was previously applied to the same label node antd reuses as the tooltip's title, making the tooltip pop up empty). antd's own `<Layout.Sider>` is back as the actual collapse mechanism — a brief attempt to replace it with a plain `<div>` (to dodge an antd cssinjs staleness bug) broke the Layout's row/column flex detection entirely, since `Layout` only switches to a side-by-side row layout when it recognizes a real `Sider` child.
- [x] **English (en-US) is now a real, complete second UI language** (2026-06-25): see [[I18N]] for the full writeup. ~130 hardcoded Portuguese strings moved to `t['...']`; fixed the actual root cause (antd's `ConfigProvider` was hardcoded to `locale={ptPT}` regardless of the selected language, so antd's own pagination/date-picker strings never translated); new `tFormat()` helper for placeholders in translated strings.
- [x] **Cross-tenant authorization hole in the Admin panel** (2026-06-25, see [[SECURITY]]) — `/api/admin/*` required only `ManagerOnly` (role 1 or 2), so any paying customer's Manager could list/disable/edit every OTHER company. New `AdminOnly` policy (role 2 only); `AdminFlowTests` added (there were zero tests for this controller before).
- [x] **Error log table** (2026-06-25, see [[SECURITY]]) — `ErrorLog` entity + `ErrorHandlingMiddleware` persists every error response (4xx/409/500) to `error_logs`; `GET /api/admin/error-logs` (`AdminOnly`, paginated) to read them.
- [x] **Supabase query-performance audit** (2026-06-25) — drift detector extended to also catch missing **indexes** (it previously only checked tables/columns, so a new `HasIndex(...)` on an already-deployed entity silently never reached the DB); fixed `SaleRepository`'s date filter using `SoldAt.Year`/`.Month` (can't use an index) → range comparison + new index. Two lower-priority findings left open in [[KNOWN-ISSUES]] (dashboard's 4 round-trips, `SearchUsersAsync`'s double query).
- [x] **Test-quality audit** (2026-06-25, see [[KNOWN-ISSUES]]) — found the friend-request email/name-swap bug (above) was hidden by tests asserting only HTTP status codes, never response body content. Broader audit of "cross-tenant" suspects found the code was already correctly protected everywhere except the Admin panel — added missing regression tests for Goals/Notifications/Stages ownership boundaries.
- [x] **First public deploy** (2026-06-26) — Render (API, Frankfurt, free) + Vercel (frontend) + Neon (Postgres, Frankfurt, free; replaced Supabase after persistent cross-region Npgsql/Supavisor timeouts — Supabase itself was healthy, confirmed by a direct local query in 59ms; the Render↔Supabase network path was the problem). Live at the Render `/health` endpoint and the Vercel URL.
- [x] **Vercel SPA 404 on direct route refresh** (2026-06-26) — `OnTime_Frontend/vercel.json` was missing entirely, so any direct navigation to a client-side route (e.g. `/dashboard`) 404'd instead of falling through to `index.html`. Added a catch-all rewrite.
- [x] **VehicleProposalTable: single-color auto-fill** (2026-06-26) — when a version has exactly one external/internal color option, the color `Select` now auto-fills and disables, mirroring the existing single-brand lock pattern. New Proposal modal widened 720px → 1040px (table columns were cramped).
- [x] **New "Como Funciona" / How It Works page** (2026-06-26) — `HelpPage.tsx`, route `/help`, sidebar entry under Configuração. Static visual walkthrough (AntD `Steps`) of the 5 main flows: Pipeline de Clientes, Propostas, Vendas, Objetivos, Amigos. New i18n keys `PAGE.HELP.*`/`NAV.HELP` in both locales + fallback. Later expanded to a 7-section `Collapse` layout (added Notificações, Veículos & Marcas) with real step-by-step bullets instead of one-sentence summaries.
- [x] **Stages page redesign** (2026-06-26) — each Funil row was cramped (name, count, badges, and an always-visible notification description line all fighting for the same row height). Replaced the description line with a `BellOutlined` icon (filled/blue when the stage has notification templates, grey outline when not) + `Badge` count, opening a `Popover` with the template list on hover/click. Initially shipped with `trigger="hover"` only, which never opens on touch devices — fixed same day to `trigger={['hover', 'click']}`.
- [x] **Home screen (Dashboard) widgets are now clickable** (2026-06-26) — KPI cards navigate to the matching list page (Clientes/Propostas/Vendas); Hot Deals table rows navigate to the client's detail page. `NAV.DASHBOARD`/`PAGE.DASHBOARD.TITLE` renamed pt-PT "Ecrã Inicial" / en-US "Home" (route stays `/dashboard`) — reserves "Dashboard" as the name for a future, separate analytics page (see [[ROADMAP]] Phase 5).
- [x] **`KPI.HOT_DEALS` kept in English in pt-PT** (2026-06-26) — "Hot Deals" is industry jargon salespeople already use; translating it to "Negócios Quentes" was worse than leaving it untranslated. Pattern to repeat for other domain jargon if requested.
- [ ] **Phase 1.5 UX/admin backlog opened** (2026-06-26, see [[ROADMAP]]) — 8 user-requested items (Goals/home redesign, optional-proposal-on-client-creation, Lead Source CRUD screen, non-automotive profile toggle, broken Admin expanded-row layout, self-service email/profile edit, Admin-only role changes + registration-always-Salesperson, optional license-plate field) scoped via `/grill-me` before being recorded — none implemented yet.
- [x] **Won/Lost stage badge bug** (2026-06-27) — `StagesPage`'s badge logic only checked `isWon` (`stage.isFinal && (isWon ? "Venda" : "Perdido")`), so a Final stage that was neither Won nor Lost always rendered "Perdido". Fixed to gate on `(isWon || isLost)` directly. Also found `CreateStageRequest` never accepted `IsFinal`/`IsWon`/`IsLost` at all (only `UpdateStageRequest` did) — a brand-new stage could never be created as Won/Lost, only edited into it afterward. Fixed both; demotion-of-previous-holder logic mirrored from `UpdateAsync`.
- [x] **Optional proposal on client creation** (2026-06-27) — `CreateClientRequest.HasProposal` (default `true`, backward compatible); when `false`, `ClientService.CreateAsync` skips creating any `Proposal`/`ProposalVehicle` rows entirely. Frontend: `NewClientPage` gained a "Já fez proposta?" switch that conditionally renders the whole Proposta card.
- [x] **Vehicle delivery tracking** (2026-06-27) — `Sale.EstimatedDeliveryDate`/`DeliveredAt` (both nullable, independent of the Won/Sale pipeline stage — "delivered" is informational, never a funnel stage per explicit user decision). Set at convert-to-sale time (estimated) and editable/markable after (`SaleDetailPage`: status tag + one-click "Marcar como Entregue").
- [x] **Editable Notes (Obs)** (2026-06-27) — Client gained `Notes` end-to-end (`ClientDto.Notes`, new `PUT /api/clients/{id}` + `UpdateClientRequest`, edit card on `ClientDetailPage`'s Informação tab). Proposal's existing edit modal gained a Notes field (the entity already had `Notes` via `BaseEntity`, just never exposed).
- [x] **Doc correction: proposals are NOT limited to one Active per client** (2026-06-27) — the "Como Funciona" page's Propostas section claimed otherwise; verified in code (`ProposalService.CreateForClientAsync` has no such check) and corrected the copy in both locales + fallback.
- [x] **"Nova Oportunidade" button** (2026-06-27) — re-engaging an already Won/Lost client (new proposal, new sale cycle) already worked mechanically with no backend guard, but had no obvious UI entry point. Added a button on `ClientDetailPage`, visible only when `currentStageIsFinal`, that moves the client to the lowest-`Order` non-final stage in one click.
- [ ] **Recurring notification templates** — fully designed, not implemented. See [[NOTIFICATIONS]] and [[ROADMAP]] Phase 4.
- [x] Latent test-suite bug found while adding a new client test: `TenantIsolationFlowTests.cs` had local shadow types `ClientDto`/`UserListDto`/`PagedResult` (same names as the real DTOs, same namespace) that silently won over the real `using`-imported types for any property both shapes happened to share — meaning that file's assertions were checking an incomplete/wrong shape this whole time without anyone noticing (it never touched a field missing from the shadow). Removed the shadows, fixed the file to use the real `ClientListDto`/`UserListDto`. No assertions in that file were actually wrong, but it's a sign to grep for other accidental same-name local types in `OnTime.Tests` if a similar "field doesn't exist" error shows up unexpectedly.
- [x] **Proposal discount bug** (2026-06-27) — `Proposal.Discount` was only ever set from a manual top-level field; the "Nova Proposta" flow (`ClientDetailPage`) never had that field at all, only per-vehicle discounts in `VehicleProposalTable`, so the discount silently never saved on create (worked when editing because `ProposalDetailPage`'s edit modal does have the manual field). Fixed by mirroring how `ProposalValue` already auto-sums vehicle prices: added `ComputeDiscount` (sums per-vehicle `Discount` when any vehicle has one set, falls back to the manual field otherwise), applied in both `BuildProposal` (create) and `UpdateAsync`. Regression test added.
- [x] **Sales page: delivery status filter + column** (2026-06-27) — `SaleListDto`/`SaleFilterParams` gained `EstimatedDeliveryDate`/`DeliveredAt`/`Delivered` (server-side filter, not client-side). `SalesPage` got a Segmented control (Todas/Por Entregar/Entregue) and a status `Tag` column (desktop, tablet, mobile card).
- [x] **"Marcar como Entregue" now asks for the date** (2026-06-27) — previously hardcoded to `new Date()`; `SaleDetailPage` now opens a small modal with a `DatePicker` (defaults to today, editable) before calling the update.
- [ ] **Configurable home-screen widgets** — noted as a future idea, not implemented. See [[ROADMAP]] Phase 5.
- [x] **Proposal detail vehicle list missing version/colors** (2026-06-27) — `ProposalDetailPage`'s vehicle card only showed brand + model (e.g. "XPENG Mona M03"), dropping `versionName`/`externalColor`/`internalColor` even though `ProposalVehicleDto` already carries them. Added a secondary line.
- [x] **Vehicle versions were create/delete-only in the UI** (2026-06-27) — backend `UpdateVersionAsync`/`PUT .../versions/{id}` and the frontend `vehiclesApi.updateVersion` already existed; only `VehiclesPage`'s version drawer never wired an edit action. Added an edit button per row, form now toggles between Add/Edit.
- [x] **Single-option auto-lock extended to Version** (2026-06-27) — `VehicleProposalTable`'s `VersionSelect` now auto-fills and disables when a model has exactly one version, same pattern as brand/color. Documented as an app-wide rule (not case-by-case) in [[CONVENTIONS]] "Single-option auto-lock".
- [x] **CI + branch protection live on both repos** (2026-06-27) — `gh` CLI installed (winget) and authenticated; `main` on both `OnTime_Backend` and `OnTime_Frontend` now requires the `build-and-test` check to pass before merge. See [[CI-CD-WORKFLOW]]. Merging is still always a manual, explicit user action — no auto-merge.
- [x] **"Template" wording removed from Notification Settings** (2026-06-27) — `ACTION.TEMPLATE.ADD/EDIT/DELETE` retitled "Adicionar/Editar/Eliminar Notificação" in both locales + fallback; the word "template" never appears in the UI now.
- [x] **Notification Settings stage picker no longer hides stages with an existing notification** (2026-06-27) — `availableStages` used to filter out any stage that already had one template, which was wrong now that a stage can have any number of notifications (already true before this fix, just never reflected in this dropdown). Now shows every non-final stage, always.
- [x] **Free-text vehicle proposal showed `vehicleName: " "`** (2026-06-27) — see [[KNOWN-ISSUES]] for the root-cause writeup (`fn_get_proposals_paged`'s `CONCAT`-of-NULLs quirk). Regression test added.
- [x] **Stale "Proposal can be created with no vehicle" doc entry corrected** (2026-06-27) — this was already fixed (T14) but [[KNOWN-ISSUES]] still listed it open; verified in source and ticked.
- [x] **Roadmap split into Sprint 1 / Sprint 2** (2026-06-27) — Phase 2 (Stripe/Ifthenpay payments) explicitly deferred to Sprint 2 at the user's request; everything else (Phase 1/1.5) is Sprint 1.
- [x] **Admin panel expanded-row layout fixed** (2026-06-27) — the Filiais sub-table now renders inside its own `Card` with proper margin + `scroll={{x:'max-content'}}` instead of a bare `<Table>` overlapping the row above it. Outer companies table also gained horizontal scroll.
- [x] **Back-button/title header alignment fixed across 4 detail/creation pages** (2026-06-27) — `ClientDetailPage`, `NewClientPage`, `ProposalDetailPage`, `SaleDetailPage` all had the `ArrowLeftOutlined` icon button visually misaligned against the adjacent `Typography.Title`. Standardized: `size="large"` + flex centering on the button, `!mt-0` on the title.
- [x] **"Já fez proposta?" now defaults to off** (2026-06-27) — `NewClientPage`'s `hasProposal` switch defaulted to `true`, which defeated the point of the toggle (quick client-only adds are the more common case). Now defaults `false`.
- [x] **Optional license-plate per vehicle row** (2026-06-27) — see [[ROADMAP]] Phase 1.5, now ticked. `ProposalVehicle.Plate` end-to-end + convert-to-sale plate inheritance from the preferred vehicle. 2 regression tests added.
- [x] **Profile shows the user's own role; registration never creates Admin** (2026-06-27) — verified `RegisterManagerAsync`/`RegisterSalespersonAsync` never assign `UserRole.Admin` (already test-covered). `ProfilePage`'s Subscription card gained a dedicated "Função" row (was incorrectly folded into "Plano", which only showed role text for Admins — now both rows always show their own thing).
- [x] **Admin panel: change a user's role** (2026-06-27) — `AdminController` gained `GET companies/{id}/users` + `PATCH users/{id}/role`, both `AdminOnly`. Self-role-change blocked (`CANNOT_CHANGE_OWN_ROLE`, 422) both backend and via a disabled `Select` on the matching row in the new `UsersSubTable` (shown alongside `BrandsSubTable` in the expanded company row). 164/164 backend tests green.
- [x] **Vehicle catalog redesigned: per-user, not global-with-filter** (2026-06-27) — see [[2026-06-27-per-user-vehicle-catalog]] and [[USER-BRANDS]] for the full design. New `UserVehicleModel`/`UserVehicleVersion` entities, FK'd to `UserId`; `ProposalVehicle`/`Sale` now point here instead of the global `VehicleModel`/`VehicleModelVersion`. New `VEHICLE_MODEL_IN_USE` (422) blocks deleting a model/version referenced by the user's own proposal/sale. `VehiclesController` model/version writes dropped `ManagerOnly` — every role manages their own catalog; brand CRUD (the shared shelf) stays `ManagerOnly`.
- [x] **Vehicle brands moved from per-user to per-Filial + multi-Filial membership** (2026-06-27, same day) — see [[2026-06-27-filial-vehicle-brands-and-membership]] and [[USER-BRANDS]]. `BrandVehicleBrand` (Filial-level, Manager/Admin-managed) replaces `UserVehicleBrand`; cloning into a user's personal catalog is now lazy (first view/create) instead of an explicit per-user "select brand" action. New `UserBrandMembership` lets a user belong to several companies/filiais — `User.CompanyId`/`BrandId`/JWT claims stay singular ("currently active" Filial); `POST /api/users/me/switch-brand` re-mints a JWT. Admin grants membership in any company, Manager only within their own. Frontend: Sidebar header's Filial name becomes a switcher `Dropdown` once a user has >1 membership; "Marcas que Vendo" removed from `ProfilePage`, moved into a `BrandsPage` drawer alongside a basic membership-grant control; `AdminPage`'s `UsersSubTable` gained an "Atribuir Filial" action. `BrandVehicleBrandsFlowTests.cs` + `MembershipFlowTests.cs` (13 new tests); 4 pre-existing tests updated. 173/173 backend tests green. **Manually verified end-to-end in the browser (2026-06-29)**: brand config save, lazy clone visible in Veículos, brand filter scoped to the Filial, configured-only proposal picker, live Filial switch via the sidebar dropdown, isolated catalog per Filial. Found and fixed one real bug along the way: `MSG.EMPTY_STATE.VEHICLES` was missing from both i18n locales (showed the raw key instead of "Nenhum veículo encontrado.").
- [x] **"Configurado" filter on Veículos, defaults to configured-only** (2026-06-29) — see [[VEHICLES]]. `VehicleSearchParams.Configured` filters server-side; `VehiclesPage` filter bar gained a "Configurados/Não Configurados/Todas" `Select` defaulting to "Configurados" on load. 1 new backend test, 174/174 green. Verified in browser after a hard refresh (stale bundle cache, not a bug).
- [x] **Data layer fully migrated to C#/EF Core, PostgreSQL stored functions retired** (2026-06-29) — see [[2026-06-29-data-layer-migrated-to-csharp]]. The 7 still-used `fn_*` functions (clients/proposals/notifications/dashboard reads) rewritten as EF Core LINQ in `ClientRepository`, `ProposalRepository`, `NotificationRepository`, `SaleRepository`; `DatabaseFunctions.cs` (56 functions, only 7 ever used) deleted entirely; `DatabaseInitializer` no longer applies any SQL at startup beyond `EnsureCreated`. Hit and fixed one real EF Core translation gotcha: `GroupBy().Select(dto).OrderBy(dto.Prop)` doesn't translate to SQL — fixed by materializing the group-by first, sorting client-side. 192/192 backend tests green; docker rebuild of the API container confirmed clean startup with no SQL-function step.
- [x] **Pagination/query-efficiency audit (2026-06-29)** — see [[ARCHITECTURE]] "Pagination". Confirmed already correct: every list endpoint defaults to `PageSize=20` with a server-side `Math.Clamp(..., 1, 50)` cap, `.AsNoTracking()` everywhere, count+skip/take pattern. No changes needed.
- [x] **"Not an automotive account" toggle, landed at the Filial level (2026-06-29)** — `Brand.IsAutomotive` (default `true`), Manager/Admin-configured on `BrandsPage`'s edit form. `LoginResponseDto` and the switch-brand response both carry it. Hides "Veículos" nav + every vehicle-picker section (`NewClientPage`/`ClientDetailPage`/`ProposalDetailPage`) when off; backend also relaxes the "≥1 vehicle required" rule on new proposals for that Filial (`IUserRepository.IsAutomotiveAsync`, checked in `ClientService.CreateAsync`/`ProposalService.CreateForClientAsync`). 5 new tests (`AutomotiveAccountFlowTests.cs`), 179/179 green. Verified end-to-end in browser: toggled off → nav item gone, proposal form has no vehicle section, client+proposal created successfully with zero vehicles; toggled back on for the demo account afterward.
- [x] **IsAutomotive (and other Filial fields) now reflect live, no re-login needed (2026-06-29)** — user asked "do I need to log out/in for the menu to change?" Answer was yes at the time (the nav reads `authStore.user.isAutomotive`, a snapshot from login/switch-brand). Fixed: `authStore` gained `patchUser(partial)`; `BrandsPage`'s edit-`Brand` mutation now calls it when the edited Filial is the caller's own active one (`result.id === user?.brandId`), patching `brandName`/`brandColor`/`isAutomotive` into the cached session immediately. Verified in browser: toggled "Vende veículos?" off from `BrandsPage` without logging out — the "Veículos" nav item disappeared instantly. Same mechanism would extend to a future "rename my own Filial" UI if one's added.
- [x] **Profile self-edit: email + phone (2026-06-29)** — `PUT /api/users/me` gained an optional `Email`, guarded by `USER_EMAIL_TAKEN` (409). Fixed a real latent bug along the way: the personal-data form only ever read the cached login-time store (no `phone` field there, so it was permanently blank on load) — now fetches `usersApi.getMe()` instead. 3 new tests, 182/182 green. Verified in browser.
- [x] **Lead Source maintenance screen (2026-06-29)** — see [[ROADMAP]]. New company-scoped `LeadSourceOption` entity (`Code`, `Name`) replaces the fixed `LeadSource` enum as the source of truth; `Client.LeadSource` stays a plain `int` (now meaning "this company's `LeadSourceOption.Code`") so the SQL paged-list functions/DTOs needed zero changes. `GET /api/lead-sources` open to any company user, `POST/PUT/PATCH` `ManagerOnly`. New `LeadSourcesPage.tsx` (mirrors `BrandsPage.tsx`). Every new company auto-seeds the original 8 defaults at registration. `LeadSourceTag` now shows the option's `Name` directly (no more translated `ENUM.LEAD_SOURCE.*` key — lead sources are free text per company). 4 new tests (`LeadSourceFlowTests.cs`), 186/186 green. Verified end-to-end in browser: create/rename/deactivate a lead source, New Client picker reflects it live. Found and fixed a real pre-existing bug: `NewClientPage`'s old hardcoded `[0..6]` options array silently excluded "Outro" (code 7) from the dropdown.
- [x] **Logout requires confirmation (2026-06-29)** — "Sair" renamed to "Terminar Sessão" (`ACTION.LOGOUT`); both logout entry points (sidebar user-menu item and the drawer footer button in `Sidebar.tsx`) now open an `Modal.confirm` ("Terminar sessão?" / "You'll need to sign in again...") before calling `logout()` — no more one-click accidental logout. Verified in browser: clicking the menu item opens the confirm dialog without ending the session; confirming logs out and redirects to `/login`.
- [x] **Multi-tenant data-scoping audit (2026-06-29)** — user asked whether all records are correctly scoped per Company/Filial/User and whether renaming a Company/Brand propagates everywhere. Audited via background agent: **architecture is sound** — no entity stores a denormalized `CompanyName`/`BrandName` string column; every tenant-scoped entity carries only a Guid FK (directly, e.g. `LeadSourceOption.CompanyId`/`BrandVehicleBrand.BrandId`, or transitively via `User.CompanyId`/`BrandId`). The JWT only carries `cid`/`bid` (IDs), never names. All SQL functions in `DatabaseFunctions.cs` resolve names via JOIN at query time, so a rename via `BrandService.UpdateAsync`/the admin company-update endpoint takes effect immediately everywhere. The **one stale-snapshot point**: `LoginResponseDto.CompanyName`/`BrandName` are captured at login/switch-brand time and held client-side in `authStore` until the next login or Filial switch — cosmetic only (sidebar header, etc.), never used for scoping/authorization. Global, intentionally-unscoped tables confirmed correct: `VehicleBrand`/`VehicleModel` (shared catalog "shelf", per-user/per-Filial cloning happens downstream), `TranslationEntry` (i18n catalog). No code changes made — confirms the existing design already supports this.
- [x] **Hide "Marcas que Vendo" for non-automotive Filiais (2026-06-29)** — `BrandsPage`'s `BrandConfigDrawer` now skips the vehicle-brands `Select` entirely when `brand.isAutomotive === false` (and skips fetching `GET /api/brands/{id}/vehicle-brands` for it too — no point configuring car brands for a Filial that doesn't sell cars). The "Membros" section still shows. Verified in browser: toggled a Filial to non-automotive, opened its config drawer, only "Membros" appeared.
- [x] **Profile page: show + edit all user data (2026-06-29)** — two real gaps found and fixed:
  - **No password-change endpoint existed at all.** Added `PUT /api/users/me/password` (`ChangePasswordRequest(CurrentPassword, NewPassword)`, verified via `IPasswordHasher.Verify`, 422 `USER_CURRENT_PASSWORD_INVALID` on mismatch). New "Segurança" card on `ProfilePage` with current/new/confirm-password fields (client-side mismatch validation). 2 new tests (`ChangePasswordFlowTests.cs`).
  - **`UserService.ToDto` had two latent bugs**: `SubscriptionStatus` was hardcoded to `0` and `LastLoginAt` was hardcoded to `null` instead of reading `u.SubscriptionStatus`/`u.LastLoginAt` — meaning `GET /api/users/me` always reported "Trial" and never showed a last-login date. Also, `User.LastLoginAt` was **never written anywhere** (dead column) — `AuthService.LoginAsync` now sets it to `UtcNow` on every successful login. New "Dados da Conta" card on `ProfilePage` shows Empresa, Filial, Membro desde, Último acesso (all read-only — these come from registration/admin, not self-editable). Verified in browser: changed password, logged in with the new one, confirmed "Último acesso" updated to the real login timestamp.
  - 188/188 backend tests green; `tsc --noEmit` clean; i18n key parity confirmed (455 keys in both `PtPT()`/`EnUS()`).
- [x] **Registration page: better guidance (2026-06-29)** — `RegisterPage` (manager sign-up) was a bare field list with zero context. Added: an intro paragraph explaining what registering creates (Company + first Filial, 14-day trial, no card), a "Os teus dados"/"A tua empresa" section split via `Divider`, a password hint ("Mínimo 8 caracteres"), and `extra` hints under Company Name and Stand Name explaining the Empresa vs Filial distinction (with examples) — this distinction had caused real confusion earlier in the session's own redesign work, so it's worth spelling out for new users too.
- [x] **"Tem Trade-In?" hidden for non-automotive Filiais (2026-06-29)** — `NewClientPage`'s trade-in toggle + fields (plate/brand/model/year/km/estimate) only make sense when the Filial actually sells vehicles; now wrapped in the same `user?.isAutomotive !== false` check already used for the vehicles section. (`ProposalDetailPage`'s read-only trade-in `Descriptions` block is left alone — it only displays data already saved on an existing proposal, gated on `proposal.tradeIn` being present, not a creation toggle.) Verified in browser: toggled the Filial to non-automotive, opened "Novo Cliente" → "Já fez proposta?" → no Trade-In toggle shown; toggled back to automotive → reappears.
- [x] **`isAutomotive` reflects live, no re-login needed (2026-06-29)** — `authStore` gained `patchUser(partial)`; `BrandsPage`'s edit-Brand mutation calls it when the edited Filial is the caller's own active one, patching `brandName`/`brandColor`/`isAutomotive` into the cached session immediately. Verified in browser: toggled "Vende veículos?" off from `BrandsPage` without logging out — the "Veículos" nav item disappeared instantly.
- [x] **Daily digest flagged as not-yet-built, kept in Sprint 1 backlog (2026-06-29)** — see [[ROADMAP]] Phase 1.5. `DigestEnabled`/`DailyDigestTime` are real, editable fields but nothing in the backend reads them (no scheduler/background job exists in the codebase at all). Documented, not implemented yet.
- [x] **Goals page + Dashboard widget visual redesign (2026-06-29)** — see [[ROADMAP]] Phase 1.5. New shared `GoalCard.tsx` (gradient backdrop, circular progress ring, escalating motivational tag, gold trophy styling at 100%, days-left countdown) used by both `/goals` and the Dashboard's pinned-goals widget. `/goals` also gained a gradient hero header. Fixed a real mistranslated label (`LABEL.GOAL.PINNED` said "★ Início" instead of "Fixado"). `tsc` clean, i18n parity 464/464, verified visually in browser (dark mode).
- [x] **Large polish batch (2026-06-29)** — nine items from one user request, all verified in browser, 190/190 backend tests green, i18n parity 482/482:
  - **Goals: manual reorder.** `UserGoal.SortOrder` (int) + `PUT /api/goals/reorder` (`ReorderGoalsRequest`); `/goals` gained ◀▶ buttons per card (`moveGoal` splices the ordered-id array and calls reorder) — left/right because cards sit side-by-side in the grid, not stacked (fixed from an initial ▲▼ that pointed the wrong way). Order persists and also drives the Dashboard's pinned-goals widget. New `GoalFlowTests.Goals_Reorder_ChangesListingOrder`.
  - **Goals: dropped the lowest motivational tier.** `motivationKey()` now returns `null` below 20% progress instead of always showing "Vamos a isso!" — most goals sit in that range for most of their period, so it was on almost every card. `GoalCard` skips the tag entirely when null.
  - **Vehicles "Todos" filter — real bug fixed.** The Select used `value: undefined` for "all", which AntD can't render as a selected option (shows empty/stuck instead). Fixed with a string sentinel (`'all'|'true'|'false'`) converted to `boolean|undefined` for the API. Default changed from "Configurados" to "Todos" per request.
  - **Logout confirm modal ignored dark mode.** Static `Modal.confirm` mounts outside `ConfigProvider`'s theme context. Fixed by using `App.useApp().modal` (the app already wraps everything in antd's `<App>` per `App.tsx`) instead of the static import.
  - **Password complexity enforced.** New `StrongPasswordAttribute` (backend, `OnTime.Application.Common`) — min 8 chars + upper + lower + digit + symbol — replaces `[MinLength(8)]` on `RegisterManagerRequest`/`RegisterSalespersonRequest`/`ChangePasswordRequest`. Frontend mirrors the same regex in `utils/password.ts`, applied to `RegisterPage` and `ProfilePage`'s password fields.
  - **Stand "Membros": real name/email search.** `BrandConfigDrawer`'s raw User-ID `Input` replaced with an `AutoComplete` reusing `friendsApi.search` (same cross-tenant, 2+ char, masked-email search FriendsPage already uses) — selecting a result calls `grantMembership` with the resolved `userId`.
  - **Subscription: shows expiry, locks down the app when expired.** `ProfilePage`'s subscription card was a hardcoded-Portuguese block with several i18n violations — rewritten with proper keys, now shows trial countdown (`trialEndsAt`) while trialing or `expiresAt` once on a real plan, plus `daysUntilExpiry` and a translated expired-state hint. **New enforcement**: `ProtectedRoute` redirects every route except `/profile` to `/profile` when `user.accountStatus === 2` (Expired) and the caller isn't Admin (which bypasses everywhere per CLAUDE.md rule #6); `Sidebar`'s nav menu collapses to empty in that state too (Profile stays reachable via the user-menu dropdown, which is separate from the main nav).
  - **Terminology unified to "Stand"/"Stands" everywhere.** Backend (`I18nController.cs`, both locales) and frontend (`i18nFallback.ts`) had a mix of "Filial"/"Filiais" (pt) and "Branch"/"Branches" (en) alongside "Stand" — now consistently "Stand"/"Stands" in both locales. Underlying C# class names (`Brand`, `BrandService`, etc.) were **not** renamed — presentation-only change, per the user's explicit "troque o nome" (rename the displayed name) ask.
  - **"Como Funciona" reviewed and expanded.** Updated `PAGE.HELP.VEHICLES_BODY` (was describing the old per-user brand-selection flow, removed in the 2026-06-27 redesign) and `PAGE.HELP.GOALS_BODY` (added the reorder mention). Added two new sections: "Stands" (multi-Stand, IsAutomotive toggle, Members search, Lead Sources) and "Conta & Subscrição" (password rules, expiry, logout confirmation).
  - **Registration: IsAutomotive at signup + multi-Stand note.** `RegisterManagerRequest` gained `IsAutomotive` (default `true`), applied to the created Brand instead of only being editable afterward; `RegisterPage` gained the same "Vende veículos?" `Switch` used on `BrandsPage`'s edit form, plus a hint that the Company can have several Stands created/joined later. New test `AutomotiveAccountFlowTests.Register_CanSetIsAutomotiveFalse_AtSignup`.
  - **Process correction mid-session**: discovered `npx tsc --noEmit` (no `-p` flag) is a silent no-op in this repo — the root `tsconfig.json` has `"files": []` and only works via project references. Always use `npx tsc -p tsconfig.app.json --noEmit` (saved to memory; matches the `ontimecrm-workflow` skill's documented command). Two real type errors from this batch (`selected.id` should've been `selected.userId`; a removed-then-still-used `Input` import) had slipped through several "clean" checks before this was caught.

- [x] **Follow-up fixes on the polish batch (2026-06-29, same day)** — user caught several things in the first pass:
  - **Goals reorder arrows pointed the wrong way.** Cards sit side-by-side (`Row`/`Col` grid), so up/down (▲▼) didn't match how they actually move — changed to left/right (`LeftOutlined`/`RightOutlined`), including the matching `ACTION.GOAL.MOVE_LEFT`/`MOVE_RIGHT` i18n keys and the "Como Funciona" mention.
  - **"Todos" vs "Todas" gender agreement** — `LABEL.FILTER.ALL` (shared by `VehiclesPage`'s configured-filter and `SalesPage`'s delivery-status filter) was "Todas"; changed to "Todos" to agree with "modelos" (masculine), the more common case.
  - **Remaining "Filial" leaks in code comments cleaned up** — `Sidebar.tsx`, `BrandsPage.tsx`, `NewClientPage.tsx`, `VehiclesPage.tsx`, `authStore.ts`, `api-auth-user.ts`, and ~17 backend `.cs` files (comments only — `Brand`/`BrandService`/etc. class names intentionally untouched). Verified via `grep -rl "Filial"` returning empty across both `src` trees.
  - **Real bug investigated and explained: "no vehicles show on Veículos."** Confirmed via direct API calls this is **not a regression** — a brand-new Stand starts with zero `BrandVehicleBrand` rows (Manager hasn't configured which vehicle brands it sells yet), so `GetModelsAsync` correctly returns nothing; the "Marcas" tab's model counts come from the **global** `VehicleBrand.ModelCount` (the shared shelf), which is unrelated. The real problem was a confusing empty state. Fixed with a dismissible `Alert` on `VehiclesPage` (`MSG.VEHICLE.NO_STAND_BRANDS_TITLE`/`_MANAGER`/`_SALES`) shown whenever the active Stand has zero configured vehicle brands — Manager/Admin get a button straight to `/brands`; salespeople get a "ask your Manager" message. Verified in browser: alert shows with zero brands configured, disappears and all 11 Dongfeng models clone in correctly once one brand is added.
  - **Subscription expiry verified end-to-end via direct DB write** (`UPDATE users SET account_status = 2, subscription_expires_at = NOW() - INTERVAL '3 days' ...`): logging in as that user returns `accountStatus: 2`; `/profile` correctly shows "Estado: Expirado", "Expira em" with the past date, "Dias restantes: 0", and the expired-hint message; navigating to `/dashboard` or `/clients` redirects straight back to `/profile`; the sidebar nav menu renders empty (`0` items). Confirms the `ProtectedRoute`/`Sidebar` lockdown added earlier in the batch actually works, not just compiles.
  - **Encoding incident, caught and fixed.** A PowerShell `Get-Content -replace ... | Set-Content` one-liner (meant to swap ▲▼ for ◀▶ in `I18nController.cs`) mojibake-corrupted ~148 lines of accented Portuguese text across the whole file (é→Ã©, •→â€¢, etc.) — the file isn't git-tracked, so there was no `git checkout` safety net. Recovered via a cp1252→UTF-8 byte round-trip in real PowerShell, verified with a clean `dotnet build` + full test pass afterward. Saved to memory: never bulk-replace text in non-ASCII files via shell one-liners — use the Edit tool, or (if a real bulk replace across files is unavoidable) plain `sed` with an ASCII-only pattern, verified safe by spot-checking one file first.
  - 190/190 backend tests green, `tsc -p tsconfig.app.json --noEmit` clean, i18n parity 485/485, all of the above re-verified live in the browser/DB after the fixes (not just re-reading the diff).
  - **Goals icon-button hover was a near-white box in dark mode.** The ◀▶/edit buttons used a hardcoded `hover:bg-blue-50` Tailwind class — fine in light mode, but a blinding near-white box in dark mode that washed out the icon. New local `IconAction` component in `GoalsPage.tsx` sets the hover background via `theme.useToken().colorFillSecondary` on `onMouseEnter`/`onMouseLeave` instead — same pattern `Sidebar.tsx`'s user-menu trigger already uses. Verified visually (no light-box flash in a dark-mode screenshot); `grep` confirmed `hover:bg-blue-50` was the only occurrence in the whole frontend.

- [x] **Real bug found and fixed: subscription lockdown used a stale cached claim (2026-06-29)** — user reported `/profile` showing "Estado: Ativo" with 30 days left, yet every other route bounced them straight back to Profile. Root cause: `ProtectedRoute`/`Sidebar`'s expired-check read `user.accountStatus` off the **cached JWT** (`authStore`, frozen at login/switch-brand time) while `ProfilePage`'s subscription card already queried `/api/subscription` live — the two could disagree any time status changed without a fresh login (e.g. an admin extends a subscription, or — as happened here — leftover state from earlier same-session testing). Fixed by switching both `ProtectedRoute.tsx` and `Sidebar.tsx` to the same live `subscriptionApi.get()` query (`staleTime: 60_000`, fails open while loading) instead of the stale claim. Verified by deliberately staging a stale `accountStatus: 2` in `localStorage` while the DB/live status was Active — confirmed the user stayed on `/dashboard` instead of being wrongly redirected.
- [x] **Goal target range: Conversion Rate capped 0-100 (2026-06-29)** — `UserGoalService.CreateGoalAsync`/`UpdateGoalAsync` now throw `GOAL_PERCENT_OUT_OF_RANGE` (422) when `MetricType == ConversionRate` and `TargetValue > 100`; `GoalsPage`'s `InputNumber`/validation rule mirrors it client-side (`Form.useWatch('metricType')`, falling back to the goal's own stored type while editing since the selector is hidden then). 2 new tests. Verified via direct API call: posting `{metricType: 3, targetValue: 150}` returns 422.
- [x] **Table sorting added to Hot Deals and Notification Settings tables (2026-06-29)** — `DashboardPage`'s Hot Deals table (name/stage/date) and `NotificationSettingsPage`'s templates table (stage/title/days-after) were missing `sorter` on their columns, unlike every other table in the app (see [[CONVENTIONS]] "Tables must have search + sort"). Added, matching the existing pattern elsewhere (`localeCompare` for strings, numeric/date diff for the rest). `NotificationsPage`'s pending-notifications screen uses an AntD `List`, not a `Table` — no column-sort concept applies there.
- 192/192 backend tests green, i18n parity 486/486, `tsc -p tsconfig.app.json --noEmit` clean, all four re-verified live (browser DOM checks + a direct `psql` write to simulate the stale-cache scenario).

## Backend — known bugs / debt
Catalogued in [[KNOWN-ISSUES]] (dev work, not deploy-gating). Highlights: trial lockout (PendingActivation blocks even GET), hardcoded i18n. Dual data layer (dead/drifted `fn_*` vs C# services) resolved 2026-06-29 — see [[2026-06-29-data-layer-migrated-to-csharp]]. The unused `OnTime_Backend/sql/functions/*.sql` mirror files (never loaded by the app) are now even more stale and should be deleted in a future cleanup pass.

## Backend — stubbed / missing
- [ ] **Payment integration** — `SubscriptionService.InitiateAsync` throws `PAYMENT_PENDING`; no Stripe/Ifthenpay client exists. `GetStatus` returns defaults, `Cancel` is a no-op. See [[PAYMENTS]].
- [ ] **i18n is hardcoded** in `I18nController.cs` (pt-PT + en-US dictionaries). `TranslationEntry` table exists but is **never read/written** — dead. See [[I18N]].
- [ ] Email verification flow
- [ ] Webhook endpoints for payment confirmation

## Frontend — exists, has bugs
- [x] All pages, routing (lazy), auth flow, Zustand stores, TanStack Query, generic components, API layer, i18n
- [x] **Test harness** — Vitest + React Testing Library + jsdom wired; `npm run test` green (T0 done 2026-06-06)
- [x] **T6–T9 frontend bugs fixed (2026-06-06):** snooze field renamed, enum labels corrected, hardcoded PT strings moved to i18n, `brandColor`/`role` `as any` casts removed, `dueAt→scheduledFor`, `MenuPermissionDto` expanded to 4 flags

## Deployment
- [ ] Not deployed. Local docker-compose only. Targets: Render.com + Vercel + Supabase.
- See [[BEFORE-DEPLOY]] for the must-fix-before-prod checklist (no-migrations data loss, trial bug, CORS, secrets — data layer item resolved 2026-06-29, see [[2026-06-29-data-layer-migrated-to-csharp]]).

## Pricing
- Monthly 15€/user · Annual 149€/user (~17% off) · 14-day free trial, no card.

---

## Related
[[ROADMAP]] · [[BEFORE-DEPLOY]] · [[FRONTEND-ISSUES]] · [[PAYMENTS]] · [[I18N]] · [[CONVENTIONS]] · [[OnTime]]
